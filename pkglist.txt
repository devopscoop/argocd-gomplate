# Arch Linux package list for argocd-gomplate — the pacman/AUR equivalent of
# the Brewfile. Installs every CLI tool used or referenced by this repo.
#
# Note: the tools inside the image (gomplate, kubectl, helm, jq, etc.) are
# installed by the Dockerfile and run in the cluster — they are NOT local
# dependencies. Building the image only needs Docker.
#
# All packages are in the official repos, so plain pacman works (as do yay/paru):
#
# Usage:
#   grep -vE '^(#|$)' pkglist.txt | sudo pacman -S --needed -

# docker — `docker build` for the image
docker

# docker-buildx — the documented multi-platform build (`docker buildx build`)
docker-buildx

# git — cloning and contributing
git
